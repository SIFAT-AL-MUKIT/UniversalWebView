<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base App Theme -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <!--
        FIX: Splash Screen Theme
        আগে parent="AppTheme" ছিল — SplashScreen API কাজ করত না।
        এখন parent="Theme.SplashScreen" করা হয়েছে।

        windowSplashScreenBackground  → কালো background
        windowSplashScreenAnimatedIcon → মাঝখানে splash ছবি
        postSplashScreenTheme          → splash শেষে AppTheme এ switch

        ⚠️ drawable/ ফোল্ডারে "splash" নামে একটা ছবি থাকতে হবে।
           (splash.png / splash.webp / splash.xml যেকোনো format)
    -->
    <style name="AppTheme.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#FF000000</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash</item>
        <item name="postSplashScreenTheme">@style/AppTheme</item>
    </style>
</resources>
